provider_installation {
  filesystem_mirror {
    path    = "/usr/lib/terraform/providers"
    include = ["*/*", "*/*/*"]
  }
  direct {
    exclude = ["*/*", "*/*/*"]
  }
}

disable_checkpoint = true
